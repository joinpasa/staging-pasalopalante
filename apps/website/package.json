{
  "name": "@pasalopalante/website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npx tsx scripts/prerender.ts",
    "build:dev": "vite build --mode development && npx tsx scripts/prerender.ts",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@pasalopalante/shared": "*"
  }
}
