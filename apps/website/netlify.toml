[build]
  base = "apps/website"
  publish = "dist"
  command = "cd ../.. && npm install && npm run build --workspace=@pasalopalante/website"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
